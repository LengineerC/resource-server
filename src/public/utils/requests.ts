export enum FTP_REQUEST_PATHS{
    BASE="ftp",
    LOGIN="login",
    LOGOUT="logout",
    LS="ls",
    GET="get",

    PREVIEW="preview",
    DOWNLOAD="download",
};

export enum CONFIG_REQUEST_PATHS{
    BASE="config",
    GET="get",
}